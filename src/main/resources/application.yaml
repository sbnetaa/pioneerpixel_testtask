spring:
  application:
    name: pionerpixel-testtask
  datasource:
    url: jdbc:postgresql:// # указать!
    username: # указать!
    password: # указать!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: localhost
    port: 6379
  elasticsearch:
    uris: http://localhost:9200
server:
  port: 8080
app:
  jwtSecret: # указать!
  jwtExpirationInMs: 86400000 # 24 часа